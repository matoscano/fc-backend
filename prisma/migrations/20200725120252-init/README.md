# Migration `20200725120252-init`

This migration has been generated by Miguel Angel at 7/25/2020, 12:02:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."BalanceTransaction" (
"id" text  NOT NULL ,
"createAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"amount" Decimal(65,30)  NOT NULL ,
"transfer" text   ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Movie" (
"id" text  NOT NULL ,
"createAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"title" text  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Shareholder" (
"id" text  NOT NULL ,
"createAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"firstName" text   ,
"lastName" text   ,
"address" text   ,
"iban" text   ,
"movie" text   ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Transfer" (
"id" text  NOT NULL ,
"createAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
"amount" Decimal(65,30)  NOT NULL ,
"description" text   ,
"movie" text   ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."_BalanceTransactionToShareholder" (
"A" text  NOT NULL ,
"B" text  NOT NULL )

CREATE UNIQUE INDEX "_BalanceTransactionToShareholder_AB_unique" ON "public"."_BalanceTransactionToShareholder"("A","B")

CREATE  INDEX "_BalanceTransactionToShareholder_B_index" ON "public"."_BalanceTransactionToShareholder"("B")

ALTER TABLE "public"."BalanceTransaction" ADD FOREIGN KEY ("transfer")REFERENCES "public"."Transfer"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "public"."Shareholder" ADD FOREIGN KEY ("movie")REFERENCES "public"."Movie"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "public"."Transfer" ADD FOREIGN KEY ("movie")REFERENCES "public"."Movie"("id") ON DELETE SET NULL ON UPDATE CASCADE

ALTER TABLE "public"."_BalanceTransactionToShareholder" ADD FOREIGN KEY ("A")REFERENCES "public"."BalanceTransaction"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."_BalanceTransactionToShareholder" ADD FOREIGN KEY ("B")REFERENCES "public"."Shareholder"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200725120252-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,47 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model BalanceTransaction {
+  id          String        @default(cuid()) @id
+  createAt    DateTime      @default(now())
+  amount      Float
+  transfer    String?
+  Transfer    Transfer?     @relation(fields: [transfer], references: [id])
+  Shareholder Shareholder[] @relation(references: [id])
+}
+
+model Movie {
+  id          String        @default(cuid()) @id
+  createAt    DateTime      @default(now())
+  title       String
+  Shareholder Shareholder[]
+  Transfer    Transfer[]
+}
+
+model Shareholder {
+  id                 String               @default(cuid()) @id
+  createAt           DateTime             @default(now())
+  firstName          String?
+  lastName           String?
+  address            String?
+  iban               String?
+  movie              String?
+  Movie              Movie?               @relation(fields: [movie], references: [id])
+  BalanceTransaction BalanceTransaction[] @relation(references: [id])
+}
+
+model Transfer {
+  id                 String               @default(cuid()) @id
+  createAt           DateTime             @default(now())
+  amount             Float
+  description        String?
+  movie              String?
+  Movie              Movie?               @relation(fields: [movie], references: [id])
+  BalanceTransaction BalanceTransaction[]
+}
```


